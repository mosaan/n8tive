# Disable script ignoring (allow build scripts)
ignore-scripts=false

# Auto-install peers
auto-install-peers=true

# Use hoisted node_modules structure for electron-builder compatibility
node-linker=hoisted
